function readDir() {
  // var fs = require("fs");
  // var files = fs.readdirSync("./");
  // console.log(files);
  // console.log(process.cwd());
  return { dir: "hey" };
}

module.exports = {
  readDir,
};
